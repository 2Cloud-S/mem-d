# V0.5.1 Reproducibility Status

## Scope checked

- Fixture references in benchmark-related tests:
  - `tests/test_dataset_quality.py`
  - `tests/test_preprocess_longmemeval.py`
  - `tests/test_benchmark_workflow.py`
- Referenced fixtures:
  - `tests/fixtures/longmemeval_audit_sample.jsonl`
  - `tests/fixtures/longmemeval_questions_sample.jsonl`

## Findings

1. The two LongMemEval sample fixtures are present and were not renamed.
2. Tests are not using generated dynamic fixtures for these paths; they are static files in `tests/fixtures/`.
3. Benchmark workflow code itself is runnable; issue found was test-scope drift:
   - `test_cli_audit_dataset_directory_output` audited the full `tests/fixtures` directory.
   - The directory now also contains non-memory gold fixtures (`evolution_gold.json`, `lifecycle_gold.json`), which are not valid memory exports for `audit-dataset`.
   - This made the test fail with `Record 1 has no content`.

## Minimal fix applied

- Updated `tests/test_dataset_quality.py` to audit only the intended benchmark sample fixtures:
  - `longmemeval_audit_sample.jsonl`
  - `longmemeval_questions_sample.jsonl`

This keeps the test intent intact while avoiding unrelated fixture contamination.

## Verification

Ran:

```bash
python -m pytest tests/test_benchmark_workflow.py tests/test_dataset_quality.py tests/test_preprocess_longmemeval.py -q
```

Result after fix: passing (20 passed).

